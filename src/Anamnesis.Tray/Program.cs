namespace Anamnesis.Tray;

internal static class Program
{
    [STAThread]
    private static void Main()
    {
        // O agente de bandeja será responsável por calendário, OBS e fontes de áudio.
        // A implementação de interface inicia após os contratos de captura serem testados.
    }
}
